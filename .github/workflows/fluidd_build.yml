name: Fluidd Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3      
    - name: Build the Docker image    
      working-directory : ./frontend
      run: DOCKER_BUILDKIT=1 docker build . --file Dockerfile --tag anetdev/fluidd:$(date +%s)
